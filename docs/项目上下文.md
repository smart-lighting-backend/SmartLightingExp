# 项目上下文

## 项目背景

智慧路灯实验项目，基于重庆交通大学与中软国际人才培养集中实训方案，构建一套面向市政道路照明的物联网管理系统。通过传感器采集环境数据、MQTT 远程通信、后端自动策略控制，实现路灯的智能开关、调光、告警与数据分析。

## 技术栈

| 分类 | 技术 | 版本 |
|------|------|------|
| 语言 | Java | 17 |
| 框架 | Spring Boot | 3.4.3 |
| ORM | MyBatis-Plus | 3.5.9 |
| 数据库 | MySQL (阿里云 RDS) | 8.4 |
| 时序数据库 | TDengine | 3.3.2.0 |
| 消息协议 | MQTT (EMQX) | 6.2.1 Enterprise |
| 认证授权 | JWT (jjwt 0.12.6) + RBAC | — |
| 密码加密 | BCrypt + AES-256-CBC | — |
| 接口文档 | Knife4j (OpenAPI 3) | 4.5.0 |
| 构建工具 | Maven | — |
| 部署 | Docker / 阿里云 ECS | — |

## 项目结构

```
SmartLightingExp/
├── database/           # SQL 脚本（建表、种子数据、Schema 升级）
├── docs/               # 项目文档（含移动端接口文档、开发参考、原型图等）
├── 配置相关/            # 配置记录笔记
├── src/main/java/com/experiment/smartlightingexp/
│   ├── common/          # 通用组件（Result 统一响应、BusinessException、GlobalExceptionHandler、SecurityContext、RequirePermission 注解）
│   ├── config/          # 配置类（CorsConfig、Knife4jConfig、JacksonConfig、MyBatisPlusConfig、JwtInterceptor/JwtProperties、MqttConfig/MqttProperties、MaxKbProperties、MyMetaObjectHandler、PasswordConfig、WebMvcConfig） + logback-spring.xml
│   ├── controller/      # 控制器（Auth、Device、DeviceArea、Alarm、AlarmRecord、Assistant、AuditLog、Control、Dashboard、Menu、Permission、Policy、Role、Telemetry、User、VisionEvent、VoiceEvent、MqttAuth — 共 18 个）
│   ├── dto/             # 数据传输对象（Login/Control/Policy/Device/Alarm/Telemetry/User/Assistant 请求与响应；PolicyTestRequest、LuxThreshold、MenuTreeNode、PermissionTreeNode）
│   ├── engine/          # 策略决策引擎（DecisionEngine — 云端评估；ConditionEvaluator — 纯函数边缘评估，可迁移硬件）
│   ├── entity/          # 实体类（16 张表映射：含 DeviceCredential、Menu、DecisionLog、AuditLog、VisionEvent、VoiceEvent、RolePermission 等）
│   ├── mapper/          # MyBatis-Plus Mapper 接口（16 个，纯注解式，无 XML 映射文件）
│   ├── mqtt/            # MQTT 发布/订阅组件（MqttPublisher、MqttSubscriber、SystemEventPublisher）
│   ├── service/         # Service 接口（含 MaxKbClient、AssistantService、DeviceCredentialService 等 17 个）
│   │   └── impl/        # Service 实现（14 个实现类）
│   ├── task/            # 定时任务（MockDataGenerator、DeviceCredentialInitializer、HeartbeatMonitorTask、EnergyCalcTask、HealthScoreTask、EdgeNodeSimulator、DeviceOfflineMonitor(冗余/注释)）
│   ├── util/            # 工具类（AesUtil、JwtUtil、EventTextNormalizer、SensorValidator）
│   └── SmartLightingExpApplication.java
└── gen_doc.py           # 文档生成脚本（Markdown → Word）
└── .env                 # 敏感配置（DB_PASSWORD、JWT_SECRET、MAXKB_API_KEY 等）
```

## 核心功能模块

| 模块 | 说明 |
|------|------|
| 设备管理 | 路灯台账，记录设备身份、状态、位置、区域(area)、最新遥测快照、健康分 |
| 遥测数据 | 传感器上报光照、温湿度、PM2.5、AQI、PIR、车流量等环境数据 |
| 设备控制 | MQTT 下发开/关/调光指令，支持手动和自动策略，记录指令历史 |
| 告警管理 | 离线(OFFLINE)、故障(FAULT)、健康分过低(HEALTH_LOW)三种类型，支持自动产生/恢复与手动处理 |
| 照明策略 | 基于阈值/时间/场景的自动节能规则引擎，支持优先级与条件 JSON 配置 |
| AI 视觉 | 行人/车辆/异常停留识别事件，联动安防与照明 |
| 语音交互 | 语音播报、紧急求助记录 |
| 能耗统计 | 日粒度亮灯时长、节能率、碳减排估算 |
| 操作审计 | 关键操作日志，满足 IR-11 安全可信控制 |
| 用户与权限 | RBAC 权限模型，支持用户管理、角色分配、权限控制 |
| 认证鉴权 | JWT 登录认证，拦截器校验 Token，白名单放行登录/文档/EMQX回调路径 |
| 设备 MQTT 鉴权 | 每设备独立 MQTT 凭据（用户名=deviceId，密码=出厂编号+识别码），EMQX built_in 认证 + BCrypt/AES 混合加密存储，SSL/TLS 传输加密，ACL Topic 隔离 |
| 仪表盘 | 设备在线率、告警统计、能耗趋势、分区(按area)数据概览 |
| 决策日志 | 策略引擎每次评估的输入快照、匹配策略、执行动作记录 |
| RAG 维修知识库 | 对接 MaxKB 知识库 API，3 份知识库文档（故障手册 + 操作指南 + 诊断维修）；三段式 AI 流程（本地正则快匹 → AI 意图识别 → RAG 问答）；一键诊断传入完整设备上下文；自然语言对话调参（7 参数白名单） |
| 设备健康评分 | 每日凌晨 2:00 综合离线频次(30%)、通信质量(25%)、指令响应率(25%)、传感器异常率(20%) 四个维度打分 (0-100)，落地 device 表 health_score 字段 |
| 菜单管理 | 动态菜单树管理（增删改查），支持按权限可见性过滤前端导航菜单 |
| 边缘 AI 决策 | 模拟路灯内置边缘节点的本地决策能力（ConditionEvaluator 纯函数 + EdgeNodeSimulator 定时任务），断网时仍可自主判断开/关/调光；Dashboard 卡片展示决策统计 + 手动触发 |
| 移动端开发 | 配套移动端接口文档（33 个接口）+ 开发参考（natapp 内网穿透、MQTT 配置、数据库连接），使用 {{BASE_URL}} 占位符支持可变地址 |
| 日志治理 | logback-spring.xml 全局日志控制（框架→WARN、业务→INFO）；MQTT 发布优化（maxInflight 200、QoS 0 模拟数据、遥测处理独立线程池）；关键异常日志中文化 |

## 数据库（MySQL 16 张表）

| 表 | 类型 | 说明 |
|---|------|------|
| **device** | 业务主数据 | 设备台账，含 rated_power(额定功率)、latest_data JSON 快照字段、健康分、手动锁定时间 |
| **device_credential** | 鉴权 | 设备 MQTT 鉴权凭证（username、BCrypt 密码哈希、AES 加密出厂编号/识别码） |
| **telemetry** | 流水 | 遥测数据（光照、温湿度、PM2.5、AQI、PIR、车流量） |
| **control_command** | 流水 | 控制指令（含来源、操作人、亮度、状态、回执时间） |
| **alarm_record** | 流水 | 告警记录（含类型、级别、状态、开始/恢复时间、处理人） |
| **lighting_policy** | 业务主数据 | 照明策略（条件 JSON、动作、优先级、生效时段） |
| **decision_log** | 流水 | 决策引擎评估日志（输入快照、匹配策略、执行动作；result 含 MATCH_EXECUTED / NO_MATCH / EDGE_MATCH_EXECUTED / EDGE_NO_MATCH 区分云端与边缘） |
| **vision_event** | 流水 | AI 视觉识别事件 |
| **voice_event** | 流水 | 语音交互事件 |
| **energy_record** | 流水 | 能耗统计（日粒度） |
| **audit_log** | 流水 | 操作审计日志 |
| **menu** | 业务主数据 | 动态菜单树（含 permission_code 权限绑定、组件路径、排序、启用状态） |
| **user** | 业务主数据 | 系统用户（含角色、部门、管辖区域、最后登录信息、软删除） |
| **role** | 业务主数据 | 角色定义（RBAC） |
| **role_permission** | 关联 | 角色-权限多对多关联 |
| **permission** | 业务主数据 | 权限定义（细粒度操作权限码） |

## 关键业务流程

1. **遥测数据采集**: 设备/模拟器通过 MQTT 发布到 `streetlight/{deviceId}/telemetry` → MqttSubscriber 写入 telemetry 表，同时更新设备心跳和最新数据快照
2. **AI 自动控制**: 每次收到遥测后触发 DecisionEngine，读取启用的照明策略，按优先级匹配条件，命中后下发 MQTT 控制指令并记录 decision_log
3. **心跳离线检测**: HeartbeatMonitorTask 每 60s 扫描一次，检测超时（默认 360s）未上报的设备，标记离线并生成告警；设备重新上报时自动恢复告警
4. **能耗统计**: EnergyCalcTask 每日 23:55 执行，读取当日 control_command 记录，按时间加权平均亮度计算节能率、用电量和碳减排量，upsert 写入 energy_record
5. **手动锁定**: 手动操作后 30 分钟内（MANUAL_LOCK_MINUTES=30），AI 策略引擎跳过该设备，防止自动策略覆盖人工干预
6. **健康评分**: HealthScoreTask 每日凌晨 2:00 对已启用设备从离线频次(30%)、通信质量(25%)、指令响应率(25%)、传感器异常率(20%) 四个维度综合打分，更新 device.health_score
7. **AI 诊断**: 运维人员在设备详情页发起诊断 → AssistantService 收集设备最新遥测/告警/控制历史 → 调用 MaxKB 知识库 API 做 RAG 问答 → 返回诊断建议或自动调参（如调整光照阈值）
8. **策略模拟测试**: 策略编辑页点击"模拟测试" → 前端实时构建 conditions JSON + 模拟传感器值 → POST /api/policies/test → DecisionEngine.matchesCondition() 评估 → 返回是否命中 + 动作 + 所有策略对比
9. **边缘 AI 决策**: EdgeNodeSimulator 每 60s 随机选取 5 台在线设备 → 读取策略缓存 + 最新遥测 → ConditionEvaluator 本地评估（与云端一致） → 写入 decision_log（EDGE_MATCH_EXECUTED / EDGE_NO_MATCH）；Dashboard 可手动触发
10. **认证鉴权**: JWT 拦截器校验所有请求（白名单除外）→ 从 DB 动态查询 user → role → permissions → 比对 @RequirePermission 注解 → 无权限 403；前端路由 beforeEach 守卫 + 按钮 v-if="hasPerm"

## 权限体系（IN-10 / IR-11 — 已全面落地）

**后端**：16 个 Controller、48 个方法全面添加 `@RequirePermission` 注解，`JwtInterceptor` 自动校验。

**前端**：16 条路由添加 `meta.permission`，`router.beforeEach` 守卫检查；7 个页面使用 `v-if="hasPerm('xxx')"` 控制按钮显隐。

**4 角色 × 17 权限码矩阵**：

| 权限码 | SUPER_ADMIN | MAINTENANCE(路灯管理员) | MUNICIPAL(市政人员) | EMERGENCY(应急人员) |
|--------|:-----------:|:---------------------:|:------------------:|:------------------:|
| dashboard:read | ✅ | ✅ | ✅ | ✅ |
| device:read | ✅ | ✅ | ✅ | — |
| device:create/update/delete | ✅ | ✅ | — | — |
| device:control | ✅ | ✅ | ✅ | — |
| **device:credential** | **✅** | **—** | **—** | **—** |
| telemetry:read | ✅ | ✅ | ✅ | — |
| energy:read | ✅ | ✅ | ✅ | — |
| alarm:read | ✅ | ✅ | ✅ | ✅ |
| alarm:delete | ✅ | ✅ | — | — |
| policy:read | ✅ | ✅ | ✅ | — |
| policy:create/update/delete | ✅ | ✅ | — | — |
| assistant:read | ✅ | ✅ | ✅ | ✅ |
| audit:read | ✅ | ✅ | ✅ | — |
| events:read | ✅ | — | — | — |
| user:* | ✅ | — | — | — |
| permission:* | ✅ | — | — | — |
| menu:* | ✅ | — | — | — |
| role:assign | ✅ | — | — | — |

- **权限注解**: `@RequirePermission("device:create")` 标注在 Controller 方法上，拦截器比对 JWT 中的 permissions 列表，无权限返回 403
- **菜单权限**: 每个菜单项绑定 permission_code，前端通过 `/api/menus/visible` 接口获取当前用户可见的导航菜单树
- **RBAC 模型**: User → Role → Permission（多对多），支持细粒度操作权限码
- **审计日志**: 所有写操作自动记录 operator + IP + 时间，满足 IR-11 追溯要求

## 配置管理

- `application.yaml` — 主配置文件（数据源、MQTT、JWT、心跳参数、MyBatis-Plus、MaxKB）
- `logback-spring.xml` — 日志级别控制（框架→WARN、业务→INFO、MqttSubscriber→WARN）
- `.env` 文件 — 敏感信息（DB_PASSWORD、JWT_SECRET、MAXKB_API_KEY、MAXKB_CHAT_COMPLETIONS_URL），通过 spring-dotenv 自动加载
- 配置前缀: `mqtt.*`, `jwt.*`, `mock.*`, `heartbeat.*`, `maxkb.*`

## 节能率（Energy Saving Rate）

### 定义
节能率 = (基准能耗 - 实际能耗) / 基准能耗 × 100%

- **基准能耗**：不采用任何节能策略时的理论能耗（即 100% 亮度全天亮灯）
- **实际能耗**：经过自适应调光、按需照明等策略控制后的实际用电量
- 数据存储在 `energy_record.saving_rate` 字段（DECIMAL(5,2)，百分比）

### 数据库结构

`energy_record` 表（日粒度设备级能耗统计）:
| 字段 | 类型 | 说明 |
|------|------|------|
| `device_id` | VARCHAR(50) | 设备ID |
| `record_date` | DATE | 统计日期 |
| `on_duration_min` | INT | 亮灯时长（分钟） |
| `avg_brightness` | DECIMAL(5,2) | 平均亮度（0~100） |
| `estimated_kwh` | DECIMAL(10,4) | 估算用电量（千瓦时） |
| **`saving_rate`** | **DECIMAL(5,2)** | **节能率（%）** |
| `carbon_reduction` | DECIMAL(10,4) | 碳减排估算（kg CO₂） |

### 当前后端计算逻辑

- **节能率**: `DashboardController.stats()` — 查询当日所有 energy_record，对 saving_rate 求均值
- **写入**: EnergyCalcTask 每日 23:55 自动执行，遍历已启用设备，读取当日 control_command 中 DIMMING 指令的亮度值，按时间加权计算平均亮度，推算用电量/节能率/碳减排，upsert 写入 energy_record
- **无调光指令时**: 默认 avg_brightness = 100%（节能率 0%），避免虚报

### 前端显示
- **Dashboard 仪表盘**：顶部统计卡片显示 `energySavingRate` + "%"，Mock 默认值 32.5%
- **数据报表页（Analytics.vue）**：展示年度总能耗、年度节省量、碳减排量、月度能耗对比柱状图、分区能耗占比饼图

---

## 设备分区（Area / 区域）

### 定义
设备通过 `device.area` 字段（VARCHAR(50)）标识所属区域，是设备台账的基础属性。区域是设备的**逻辑分组**，维度上描述设备的地理或管理归属。

### 数据库
```sql
area VARCHAR(50) DEFAULT NULL COMMENT '区域（如：A区、B区）'
```
- 有独立索引 `KEY idx_area (area)`
- 区域内可包含多个设备

### 后端分区统计
`DashboardController.districts()` — 按 `area` 字段分组统计各区域设备的在线/离线/告警数量。

### 前端分区显示方式

| 页面 | 显示方式 | 数据来源 |
|------|---------|---------|
| **Dashboard 仪表盘** | 右侧"分区设备状态"卡片：列表显示每个区域名称 + 在线/离线/告警数量 + 在线率进度条 | `GET /api/dashboard/districts` |
| **设备管理列表** | 设备表格中的"区域"列，支持按区域关键词搜索过滤 | `POST /api/devices/list` 中 `area` 参数 |
| **数据报表页** | 饼图展示分区能耗占比 | Mock 数据（高新区、市中心、工业园、学院路等） |

### Mock 示例区域值
- **后端 Mock**: A区, B区, C区, D区, E区
- **前端 Mock (dashboard.js)**: 高新区, 创业园区, 市中心, 工业园, 学院路段
- **前端 Mock (Analytics.vue 饼图)**: 高新区(32), 市中心(25), 工业园(20), 学院路(13), 其他(10)

---

## 前端项目

位置: `D:\Vue-Workspace\smart_lighting_vue`

技术栈: Vue 3 + Vite + Element Plus + ECharts + 高德地图 JS API 2.0

### 主要页面（20+ 个）

仪表盘、设备管理、设备详情、分区管理、数据报表、能耗走势、告警中心、策略配置/编辑、AI 智能助手、事件中心、系统日志、用户/角色/权限/菜单管理

### 前端工具模块

| 文件 | 说明 |
|------|------|
| `composables/useAMap.js` | 高德地图 SDK 动态加载（单例，含超时重试） |
| `composables/useAutoRefresh.js` | 页面自动刷新（支持自定义间隔、敏感操作防抖） |
| `composables/useUserInfo.js` | 用户信息与权限判断 |
| `utils/manualControlState.js` | 手动控制状态解析（控制历史 + 遥测快照） |
| `utils/coordinate.js` | 经纬度解析与校验 |
| `utils/excelTemplate.js` | Excel 导入导出模板 |
| `components/LocationPicker.vue` | 高德地图选点组件（搜索设备/地址、原位粉色标记） |
| `components/DeviceMap.vue` | 设备分布地图组件 |
| `components/BatchImport.vue` | 批量导入设备组件 |
| `components/PerceptionPanel.vue` | 融合感知面板（遥测+视觉+语音+告警） |

## 移动端开发文档

| 文件 | 说明 |
|------|------|
| `docs/移动端开发参考.md` | 服务地址（natapp/ECS）、认证方式、MQTT 配置、数据库连接 |
| `docs/移动端接口文档.md` | 33 个接口详细说明（请求/响应/分页），使用 `{{BASE_URL}}` 占位符 |
| `docs/移动端接口文档.docx` | Word 版接口文档（已嵌入全部原型图），可直接分发 |
| `docs/移动端原型图/` | 9 张移动端原型截图（登录、设备详情、控制、报表、助手等） |

## 近期重点改动（2026-07-16）— 真实硬件接入

| 类别 | 改动 | 说明 |
|------|------|------|
| 硬件 | 新增小熊派 BearPi-HM_Nano 真实设备 | `Real_CS_001`，E53_SC1 扩展板（BH1750 光照 + LED），MQTT 接入 EMQX |
| 固件 | 新增 `E1_smartlighting_device` 示例 | Flash 配置存储 + AT 串口配网 + WiFi/MQTT 连接 + 边缘决策 + 远程控制 |
| 数据库 | device 表新增 `source` 字段 | `SIMULATED`=模拟设备，`REAL`=真实硬件 |
| 后端 | MockDataGenerator 跳过真实设备 | 查询条件加 `.ne(Device::getSource, "REAL")` |
| 后端 | Device.java 新增 `source` 字段 | 实体类映射 |
| EMQX | ACL 修复 subscribe Topic | `control` → `command` + `config`，匹配后端实际下发路径 |
| EMQX | 新增 built_in 用户 `Real_CS_001` | 密码 `RealAAA123456` |

## 更早改动（2026-07-11）— 设备 MQTT 鉴权

| 类别 | 改动 | 说明 |
|------|------|------|
| 鉴权 | 新增 `device_credential` 表 | 存储设备 MQTT 凭证（username、BCrypt 哈希、AES 加密出厂编号/识别码） |
| 鉴权 | 新增 `DeviceCredentialService` | 凭证生成/密码重建/EMQX 同步 |
| 鉴权 | 新增 `MqttAuthController` | EMQX HTTP Auth/ACL 回调接口（备用） |
| 鉴权 | 新增 `DeviceCredentialInitializer` | 启动时自动为存量设备生成凭证 |
| 鉴权 | 新增权限 `device:credential` | 仅 SUPER_ADMIN 可修改设备识别码 |
| 鉴权 | 新增 `AesUtil` | AES-256-CBC 加解密工具（出厂编号/识别码可逆加密存储） |
| MQTT | 后端 + 模拟器全切 SSL | `ssl://47.96.27.141:8883`，Paho 客户端加 trustAll + disableHostnameVerification |
| MQTT | MockDataGenerator 每设备独立连接 | 每台设备用自己 username/password 发布数据 |
| MQTT | MqttPublisher 线程池优化 | SingleThread → CachedThreadPool，解决 system/alarms 超时 |
| EMQX | 认证链 | built_in_database (plain) + MySQL (bcrypt, 暂禁用) |
| EMQX | ACL 设备隔离 | `${username}` 限制设备只能操作自己的 topic |
| EMQX | SSL/WSS 监听器开放 | 安全组 + Docker 端口映射 8883/8084 |
| EMQX | TDengine 规则引擎重建 | telemetry/vision/voice 三条桥接规则（容器重建后恢复） |
| 前端 | 设备详情页"修改识别码" | 仅 admin 可见，修改后自动同步 EMQX |
| 前端 | 批量导入增加出厂编号 | 必填校验 + 重复检测，模板新增对应列 |
| 前端 | 修复返回白屏 | MainLayout 移除 Transition 外套（DeviceDetail 多根节点冲突） |
| 前端 | MQTT WebSocket 改回 WS | WSS 自签名证书浏览器不信任 |

## 更早改动（2026-07-07）

| 类别 | 改动 | 说明 |
|------|------|------|
| 告警 | 告警类型统一为 OFFLINE / FAULT / HEALTH_LOW | 去掉未实现的 SECURITY/VISION，新增 HEALTH_LOW 自动告警 |
| 告警 | HealthScoreTask 健康分<60 自动产生 HEALTH_LOW 告警 | 健康分回升自动恢复，alarmRecordService 新增 resolveHealthAlarm |
| 告警 | Dashboard 跳转 /warning?status=ACTIVE 预筛选 | 点击"未处理告警"自动筛选待处理状态 |
| MQTT | 心跳发布 QoS 0 → 1 | 减少丢包导致的离线误报 |
| DTO | TelemetryQueryRequest 加 @JsonIgnoreProperties | 兼容前端未知字段 |
| 地图 | 路灯图标全面升级 | Canvas 绘制经典路灯造型，替换原 CSS 圆点 |
| 地图 | 区域分组选择 + 闪烁动画 | DeviceMap/LocationPicker/BatchImport 三组件统一区域筛选+呼吸灯 |
| 地图 | 公共模块 streetlightIcon.js | 三个地图组件共享路灯绘制+区域配色 |
| 前端 | useAutoRefresh 修复 | Dashboard/Warning 将 useAutoRefresh 移出 onMounted 修复生命周期警告 |
| 文档 | 移动端接口文档告警类型同步 | OFFLINE/FAULT/HEALTH_LOW，CRITICAL/MAJOR/WARNING/MINOR/INFO |
| 运维 | Android 代码迁移 | codex/android-app-ui-polish 分支迁至 SmartLightingAndroid 仓库 |


移动端位置,d盘"java"文件夹下面的"SmartLightingAndroid"

## MaxKB 版本与解决方案

| 信息 | 值 |
|------|-----|
| 镜像 | `1panel/maxkb:latest` (3.15GB) |
| 版本 | latest (约 2026-07-03) |

**问题**：3 容器共存的 3.48GB ECS 上，MaxKB 的 celery 进程占用 ~600MB，被系统 OOM Killer 杀掉后触发 `start-all.sh` 连锁重启 celery → 又被杀 → 死循环；同时 Gunicorn timeout=30s 导致复杂 RAG 超时。

**三步骤解决**：
1. `docker update --memory 1.8g --memory-swap 2g maxkb` — 内存硬限制
2. 删除 MaxKB 后台 3 个大 PDF 文档 — 减少 celery 向量化压力
3. **假 celery 替身** — `docker exec` 用空 shell 脚本替换 `/opt/py3/bin/celery`，占几 KB 内存但不干活，骗过 `start-all.sh` 的 `wait -n` 进程监控；同时后端超时调大（connect 15s, read 120s）