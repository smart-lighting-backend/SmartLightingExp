# 智慧路灯节能系统 — 待实现功能评估

> 基于《智慧路灯节能系统需求规格说明书-舔砖java组 V2.0》逐项对照  
> 评估日期：2026-07-04

---

## 一、基础功能（FR-01 ~ FR-10）— 全部已实现 ✅

| # | 功能 | 对应接口/组件 | 状态 |
|---|------|-------------|------|
| FR-01 | 光照强度监测 | MQTT → Telemetry → `device.latestData` → 前端 | ✅ |
| FR-02 | 历史数据趋势 | `POST /api/telemetry/history` 按设备+时间分页 | ✅ |
| FR-03 | 光照阈值设置 | `GET/PUT /api/policies/lux-threshold` | ✅ |
| FR-04 | 光照联动自动控制 | `DecisionEngine.evaluate()` → MQTT 下发指令 | ✅ |
| FR-05 | 手动远程控制 | `ManualControlModal` + `DeviceDetail` + 手动锁定模式 | ✅ |
| FR-06 | 设备在线状态监控 | `HeartbeatMonitorTask` + `device.status` | ✅ |
| FR-07 | 设备离线告警 | `HeartbeatMonitorTask` → `AlarmRecord` → 自动恢复 | ✅ |
| FR-08 | 路灯设备管理 | Device CRUD + 分页 + 启用/禁用 | ✅ |
| FR-09 | 告警日志查看 | `GET /api/alarms/page` | ✅ |
| FR-10 | 维护知识问答 | `AIAssistant` + MaxKB | ✅ |

---

## 二、创新点实现状态总览

| # | 创新点 | 状态 | 缺失内容 |
|---|--------|------|----------|
| IN-01 | 多模态感知融合 | 🟢 已实现 | ✅ 07-04 完成：perception API + PerceptionPanel 8格网格+事件行 |
| IN-02 | AI边缘快速决策 | 🟡 部分 | 无边缘节点模拟，纯服务端决策 |
| IN-03 | 自适应智能调光 | 🟢 已实现 | — |
| IN-04 | 视觉感知与联动 | 🟢 已实现 | ✅ 07-04 完成：Controller + Mock 联动 + EventCenter 页面 |
| IN-05 | 语音告警与播报 | 🟢 已实现 | ✅ 07-04 完成：Controller + Mock 联动 + DecisionEngine 联动 |
| IN-06 | 预测性维护 | 🟢 已实现 | ✅ 07-04 完成：ACK 机制 + HealthScoreTask + 健康评分 API + 前端卡片 |
| IN-07 | 多维综合可视化 | 🟢 已实现 | — |
| IN-08 | RAG维修知识问答 | 🟡 部分 | 通用聊天非专用 RAG |
| IN-09 | 绿色低碳指标 | 🟢 已实现 | — |
| IN-10 | 安全可信控制 | 🟡 部分 | RBAC 未全面强制执行 |

---

## 三、待实现功能详细说明

### 1. IR-05 / IN-04 视觉感知事件 ✅ 已完成 (07-04)

**现状：**

- `vision_event` 表、`VisionEvent` entity、`VisionEventMapper`、`VisionEventService` 已建好
- **缺 `VisionEventController`** — 没有任何 REST API
- 前端无任何视觉事件展示页面
- MockDataGenerator 未生成视觉事件模拟数据

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端 Controller | `VisionEventController`：分页查询 `GET /api/vision-events/page`、按设备查询、事件录入 `POST /api/vision-events` |
| 后端模拟 | `MockDataGenerator` 增加视觉事件生成（随机产生行人/车辆/异常停车等事件，发布到 `streetlight/{deviceId}/vision/event`） |
| 后端订阅 | `MqttSubscriber` 订阅 `vision/event` topic，写入 `vision_event` 表，联动生成 AlarmRecord |
| 前端页面 | 新增"视觉事件"Tab 或在告警页面展示（事件类型、置信度、截图引用、发生时间） |

**工作量估算：** 约 1 天

**vision_event 表结构：**
```
id, deviceId, eventType(行人/车辆/异常停车/危险场景),
confidence(置信度), snapshotRef(截图引用), occurredAt, createTime
```

---

### 2. IR-06 / IN-05 语音告警与应急响应 ✅ 已完成 (07-04)

**现状：**

- `voice_event` 表、`VoiceEvent` entity、mapper、service 已建好
- **缺 `VoiceEventController`** — 没有任何 REST API
- 策略动作中的 `voiceAlert` 复选框只是存储，从未触发实际语音播报

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端 Controller | `VoiceEventController`：分页查询 `GET /api/voice-events/page`、事件录入 |
| 后端联动 | `DecisionEngine.executeAction()` 中，当策略命中且 `extraActions.voiceAlert=true` 时，写入 `voice_event` 表 |
| 前端页面 | 告警页面增加语音事件列表；设备详情页增加语音播报状态指示 |
| 前端通知 | 浏览器 Web Audio API 播放提示音（或 TTS 语音播报警情） |

**工作量估算：** 约 0.5 天

**voice_event 表结构：**
```
id, deviceId, type(播报/广播/警告), content(播报内容),
source(自动/手动), occurredAt, createTime
```

---

### 3. IR-07 / IN-06 预测性维护与健康评分 ✅ 已完成 (07-04)

**现状：**

- `device.health_score` 字段存在但**始终为 NULL**，从未被写入
- 无任何健康评分计算逻辑
- 无维护建议生成逻辑

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端定时任务 | `HealthScoreTask`（每天凌晨执行），综合以下维度计算 0-100 分： |
|  | ① 离线频次：近 7 天 `alarm_record` 离线次数（权重 30%） |
|  | ② 通信质量：近 24h 遥测上报间隔标准差（权重 25%） |
|  | ③ 指令响应率：`control_command` SENT/ACK 成功率（权重 25%） |
|  | ④ 传感器异常率：遥测字段为 null 或超出合理范围的占比（权重 20%） |
| 后端 API | `GET /api/devices/{deviceId}/health` 返回评分 + 各维度明细 + 下降原因 |
|  | `GET /api/devices/health/summary` 返回所有设备健康概览 |
| 前端 | `DeviceDetail` 增加健康评分卡片（环形进度条 + 分维度雷达图） |
|  | `DeviceList` 增加健康状态筛选和排序 |

**工作量估算：** 约 2 天

---

### 4. IN-01 / IR-01 多模态感知融合面板 ✅ 已完成 (07-04)

**现状：**

- Telemetry 采集了 8 个维度的环境数据
- VisionEvent / VoiceEvent 表结构完整但无数据
- 前端缺少一个集中展示所有感知维度的面板

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端 API | `GET /api/devices/{deviceId}/perception` 聚合返回最新遥测 + 最新视觉事件 + 最新语音事件 |
| 前端组件 | 新建 `PerceptionPanel.vue`，在 DeviceDetail 中增加 Tab： |
|  | 环境感知卡片（光照/温湿度/PM2.5/AQI） |
|  | 视觉感知卡片（最新识别事件缩略图 + 类型） |
|  | 雷达感知卡片（PIR/车流量） |
|  | 语音事件卡片（最新播报记录） |

**工作量估算：** 约 1 天

---

### 5. IR-04 场景策略编排完善 🟡 优先级中

**现状：**

- 策略 CRUD + 启用/禁用 + 条件编辑（今日刚修好核心 bug）
- **缺失：** 策略执行历史可视化、策略模拟测试

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端 API | `GET /api/policies/{id}/history` 返回该策略的 decision_log 执行历史 |
| 后端 API | `POST /api/policies/test` 接收模拟遥测数据，返回匹配结果（不实际下发指令） |
| 前端 | Strategy 列表增加"执行历史"按钮 → 展示折线图（何时触发、触发次数趋势） |
| 前端 | StrategyCreate 增加"测试"按钮 → 输入模拟值 → 显示是否命中 |

**工作量估算：** 约 1 天

---

### 6. IN-08 / IR-09 RAG 维修知识库强化 🟡 优先级中

**现状：**

- `AIAssistant` 通过 MaxKB 聊天，但本质是通用问答
- 未导入路灯专用维修知识库
- 未传入设备上下文（最近告警、设备型号等）

**需实现：**

| 层级 | 内容 |
|------|------|
| 知识库 | 在 MaxKB 中导入《路灯常见故障手册》和项目文档 |
| 后端 | `AssistantController` 传入结构化上下文（设备型号、最近告警列表、遥测异常值） |
| 前端 | `AIAssistant` 增加"一键诊断"按钮 → 自动填充当前设备上下文 → 发送给 MaxKB |
| 前端 | 回答中高亮引用来源文档 |

**工作量估算：** 约 1 天（主要工作在知识库整理录入）

---

### 7. IN-02 边缘AI决策模拟 🟢 优先级低

**现状：**

- DecisionEngine 完全在服务端运行
- 无边缘节点概念，无断网场景处理

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端 | `EdgeNodeSimulator` 定时任务，模拟边缘节点行为： |
|  | ① 读取本地缓存策略（无网时仍可决策） |
|  | ② 执行本地开关灯判断（光照低于阈值 → 开灯） |
|  | ③ 写入 decision_log（source=EDGE） |
|  | ④ 网络恢复后同步到云端 |
| 前端 | Dashboard 增加边缘节点状态卡片 |

**工作量估算：** 约 2 天

---

### 8. IR-11 RBAC 权限全面落地 🟢 优先级低

**现状：**

- `user`、`role`、`permission`、`role_permission`、`audit_log` 表结构完整
- JWT 认证 + 审计日志已实现
- **缺前端路由守卫和按钮级权限控制**

**需实现：**

| 层级 | 内容 |
|------|------|
| 后端 | Controller 方法增加 `@PreAuthorize` 注解（或自定义切面检查角色权限） |
| 后端 | 返回当前用户权限列表 `GET /api/auth/permissions` |
| 前端 | 路由守卫：未登录跳转登录页，无权限显示 403 |
| 前端 | 按钮级权限：管理员才看到"删除""编辑"按钮；普通用户只读 |

**工作量估算：** 约 1.5 天

---

## 四、工作量汇总

| 优先级 | 编号 | 功能 | 估算 |
|--------|------|------|------|
| ~~🔴 高~~ | ~~1~~ | ~~视觉感知事件~~ | ✅ 已完成 (07-04) |
| ~~🔴 高~~ | ~~2~~ | ~~语音告警与应急响应~~ | ✅ 已完成 (07-04) |
| ~~🔴 高~~ | ~~3~~ | ~~预测性维护与健康评分~~ | ✅ 已完成 (07-04) |
| ~~🟡 中~~ | ~~4~~ | ~~多模态感知融合面板~~ | ✅ 已完成 (07-04) |
| 🟡 中 | 5 | 策略编排完善（历史+模拟测试） | 1 天 |
| 🟡 中 | 6 | RAG 维修知识库强化 | 1 天 |
| 🟢 低 | 7 | 边缘AI决策模拟 | 2 天 |
| 🟢 低 | 8 | RBAC 权限全面落地 | 0.5 天 |
| **剩余** | | | **4.5 天** |

---

## 五、建议实施顺序

```
第1天：视觉感知事件（IR-05）+ 语音告警（IR-06）  ← 表已建好，快速出成果
第2-3天：预测性维护（IR-07）                    ← 核心创新点，赋值最高
第4天：策略编排完善（IR-04）                    ← 刚修好 bug，趁热打铁
第5天：多模态感知融合面板（IN-01）              ← 展示效果提升
第6天：RAG 维修知识库（IN-08）                  ← 验收演示加分
第7-8天：边缘AI决策（IN-02）                    ← 技术亮点
第9-10天：RBAC（IR-11）                        ← 安全兜底
```
