# 前端待办 — 能耗统计（等待前端伙伴确认后再实施）

> 创建日期：2026-07-03
> 背景：后端已完成 EnergyCalcTask 定时任务，每日 23:55 自动计算能耗数据写入 energy_record 表。
> 以下改动需要等前端伙伴确认当前分支修改范围后再进行。

## 1. Dashboard.vue — 节能率显示

- **现状**：Dashboard.vue 已从 `/api/dashboard/stats` 获取 `energySavingRate` 并显示
- **预期**：后端 stats 接口的 `energySavingRate` 已改为查询 energy_record 表 AVG(saving_rate)，数据会自动出现
- **改动量**：可能无需改动，等后端跑一次定时任务后确认数据是否正常显示即可

## 2. Analytics.vue — 能耗报表页面

- **现状**：当前使用纯 mock 数据（柱状图、饼图）
- **需要改动**：
  - 对接后端 `/api/dashboard/stats` 获取 totalEnergy/totalSaving/carbonReduction 汇总指标
  - 对接后端 `/api/energy-record/monthly?year=2026` 获取月度数据（后端需新增此接口）
  - 对接后端 `/api/energy-record/district` 获取分区能耗占比（后端需新增此接口）
- **优先级**：中（后端两个新接口也待实现）

## 3. 等待确认事项

- [ ] 前端伙伴当前分支在改什么，是否会与上述改动冲突
- [ ] Analytics.vue 的 mock 数据是否要先保留作为 fallback
- [ ] 是否需要新增一个"能耗配置"页面来设置亮灯时长等参数（当前决定暂不实现）
