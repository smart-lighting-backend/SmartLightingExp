-- ============================================================
-- 智慧路灯系统 · 数据库增量变更脚本（V2.2）
-- 适用版本: 在 04_decision_log.sql 基础上执行
-- ============================================================

-- ============================================================
-- 7. device — 新增 rated_power 字段
--    用于能耗统计中估算用电量（kWh = rated_power / 1000 × on_duration × brightness%）
--    存量设备默认 150W（常见 LED 路灯额定功率）
-- ============================================================
ALTER TABLE device
    ADD COLUMN rated_power DECIMAL(10,2) DEFAULT 150.00 COMMENT '额定功率（W），用于能耗估算'
    AFTER last_manual_at;
